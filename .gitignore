tmp/
bosfoot.log
bosfoot
.env

# Binaries (root build artifacts only — anchored so cmd/* source dirs aren't ignored)
/main
/imgopt
/imgvariants
/bosfoot
*.log

# Reservation exports — contain customer PII (emails/phones), never commit
reservations_to_order.csv
reservations_contacts.csv

