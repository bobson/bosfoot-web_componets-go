tmp/
bosfoot.log
bosfoot
.env

# Binaries (root build artifacts only — anchored so cmd/* source dirs aren't ignored)
/main
/imgopt
/imgvariants
/bosfoot
*.log

# Reservation exports — contain customer PII (emails/phones), never commit
reservations_to_order.csv
reservations_contacts.csv

# DB backup dumps (e.g. from order cleanups) — contain customer PII, never commit
backup_*.csv

# User-uploaded review photos (local dev store; prod uses UPLOADS_DIR on the box).
# Anchored to repo root so it does NOT also ignore internal/uploads/ (the Go pkg).
/uploads/


# Built test binary from `go build ./cmd/orders`
/orders
/shoeimport

# Biome lint toolchain (devDependency; package.json + lockfile are committed)
node_modules/
